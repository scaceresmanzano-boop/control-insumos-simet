# Copia este archivo a .streamlit/secrets.toml (que no se sube a git) y
# cambia el valor por la contraseña real. En Streamlit Community Cloud se
# configura en Settings -> Secrets con el mismo formato, directamente desde
# el navegador (no se sube ningún archivo).

admin_password = "cambia-esta-clave"
